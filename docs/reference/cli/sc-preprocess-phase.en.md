---
aliases:
- convert-hfss-phase
tags:
- audience/team
status: stable
owner: docs-team
audience: team
scope: HFSS Phase conversion command
version: v0.1.0
last_updated: 2026-01-28
updated_by: docs-team
---

# sc preprocess phase

Convert HFSS exported S-parameter phase CSV into the standard dataset format (DB import) (formerly `sc-preprocess-phase`).

## Usage

```bash
uv run sc preprocess phase [OPTIONS] [csv ...]
```

## Arguments

| Argument | Description | Default |
|---|---|---|
| `csv` | HFSS phase CSV path(s) | - |

## Options

| Option | Description | Default |
|---|---|---|
| `--dataset-name` | Force specific Dataset Name (single file mode only) | |
| `--tags` | Comma-separated tags | |
| `--match` | Comma-separated keywords to filter files in directory mode | `Phase,S21,deg,rad` |

## Examples

**Basic import**
```bash
uv run sc preprocess phase data/raw/layout_simulation/phase/MyChip_Phase.csv
```

## Notes

!!! note "Database import"
    The current pipeline imports into SQLite; JSON output is deprecated.

<!-- CLI-HELP-START -->

## CLI Help (Auto-generated)

!!! note "Auto-generated"
    This section is generated by `sc-docs-cli`. Do not edit manually.

```text
Usage: sc-preprocess-phase [OPTIONS] [CSV]...

 Import HFSS phase CSV to SQLite database.

╭─ Arguments ──────────────────────────────────────────────────────────────────╮
│   csv      [CSV]...  Path(s) to HFSS phase CSV.                              │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ Options ────────────────────────────────────────────────────────────────────╮
│ --dataset-name        TEXT  Override dataset name                            │
│ --tags                TEXT  Comma-separated tags for database record         │
│ --help                      Show this message and exit.                      │
╰──────────────────────────────────────────────────────────────────────────────╯
```

<!-- CLI-HELP-END -->
